const addCurrency = require('./add')
const getCurrency = require('./get')
const updateCurrency = require('./update')
const removeCurrency = require('./remove')

module.exports={
    addCurrency,
    updateCurrency,
    removeCurrency,
    getCurrency
}