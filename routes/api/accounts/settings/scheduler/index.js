const addSchedulerSettings = require('./add')
const getSchedulerSettings = require('./get')
const updateSchedulerSettings = require('./update')
const removeSchedulerSettings = require('./remove')

module.exports={
    addSchedulerSettings,
    updateSchedulerSettings,
    removeSchedulerSettings,
    getSchedulerSettings
}