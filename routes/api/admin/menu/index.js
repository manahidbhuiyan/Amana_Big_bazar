const addMenu = require('./add')
const removeMenu = require('./remove')
const updateMenu = require('./update')
const getMenu = require('./get')

module.exports={
    addMenu,
    removeMenu,
    updateMenu,
    getMenu
}